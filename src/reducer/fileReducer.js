const defautlState = {};

//Создадим стейт который примит 1 параметром стейт а 2 экшин

export default function fileReducer(state = defautlState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
